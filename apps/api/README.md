# apps/api

API Gateway — point d'entrée HTTP unique de la plateforme, route vers les
services internes (`services/*`). Remplace le FastAPI monolithique de la
V0 (`docs/architecture/v0/BACKEND_ARCHITECTURE.md`) par une architecture
gateway + services découplés.

**Statut** : non démarré.
