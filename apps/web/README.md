# apps/web

Interface créateur (Next.js App Router). Consomme `apps/api` via
`packages/sdk`, utilise `packages/ui`. Remplace et étend le frontend V0
(`docs/architecture/v0/FRONTEND_ARCHITECTURE.md`) — le portfolio y devient
une vue parmi d'autres, plus le seul livrable.

**Statut** : non démarré (V0 à réimplémenter dans cette structure, pas
repartir de zéro fonctionnellement).
