# packages/types

Types TypeScript partagés entre `apps/*`, dérivés des `schemas.py` de
chaque service backend pour garantir la cohérence front/back. Toute
divergence entre ce package et un `schemas.py` de service est un bug.

**Statut** : non démarré.
