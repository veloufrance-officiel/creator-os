# packages/ui

Design system partagé (composants React + Tailwind + shadcn/ui), consommé
par `apps/web` et `apps/admin`. Un seul endroit pour l'identité visuelle
de la plateforme.

**Statut** : non démarré.
