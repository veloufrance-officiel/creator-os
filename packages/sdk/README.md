# packages/sdk

Client typé pour appeler `apps/api` (API Gateway) depuis `apps/web` et
`apps/admin`. Aucun appel HTTP direct aux services internes en dehors de
ce SDK — tout passe par la gateway.

**Statut** : non démarré.
