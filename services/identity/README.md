# services/identity

**Rôle** : Auth, IAM, RBAC, tenants, sessions, audit.

**Tables possédées** : `users`, `tenants`, `roles`, `permissions`,
`role_permissions`, `user_roles`, `sessions`, `oauth_accounts`, `audit_logs`.

**Statut** : Sprint F1 (prochain sprint — premier service implémenté).

**Objectif Sprint F1** : un créateur peut créer un compte dans un
environnement sécurisé.

Voir `docs/architecture/DATA_MODEL.md` et `SECURITY.md` à la racine du repo.
