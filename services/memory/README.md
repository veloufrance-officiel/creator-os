# services/memory

**Rôle** : Memory Engine — mémoire IA persistante par créateur (contexte,
historique, préférences), consommée par `services/ai` et `services/creator`.

**Tables possédées** : `memory_entries`.

**Statut** : non démarré.
